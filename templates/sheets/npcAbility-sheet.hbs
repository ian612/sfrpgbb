<form class="{{cssClass}}" autocomplete="off">
    <header class="sheet-header">
        <img src="{{item.img}}" data-edit="img" title="{{item.name}}" height="64" width="64"/>
        <h1><input name="name" type="text" value="{{item.name}}" placeholder="{{ localize "sfrpgbb.general.name" }}"/></h1>
    </header>
    <div class="sheet-content">
        <table>

            <tr>
                <th>{{localize "sfrpgbb.npc.abilityType"}}</th>
                <th>{{localize "sfrpgbb.weapon.damageType"}}</th>
                <th>{{localize "sfrpgbb.npc.value"}}</th>
            </tr>

            <tr>
                <td>
                    <select name="data.type">
                        {{#select data.type}}
                        {{#each config.npcAbilityTypes as |name type|}}
                        <option value="{{type}}">{{localize name}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                </td>
                <td>
                    <select name="data.damageType">
                        {{#select data.damageType}}
                        {{#each config.damageTypes as |name type|}}
                        <option value="{{type}}">{{localize name}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                </td>
                <td>
                    <input name="data.value" type="number" value="{{data.value}}" placeholder="{{localize "sfrpgbb.npc.units" }}" data-dtype="Number" />
                </td>
            </tr>

        </table>
        <b>{{localize "ITEM.TypeNpcability"}} - {{localize "sfrpgbb.general.description"}}</b>
        {{editor content=data.description target="data.description" button=true owner=owner editable=true}}
    </div>
</form>